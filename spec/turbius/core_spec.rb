require 'spec_helper'

describe Turbius::Core do
  it 'has a version number' do
    expect(Turbius::Core::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
